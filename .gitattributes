Backend/AI_Work/Support/data/regs_json/27-10.json filter=lfs diff=lfs merge=lfs -text

# Always store shell scripts with LF line endings so they run correctly in
# Linux containers even when edited on Windows.
*.sh    text eol=lf
